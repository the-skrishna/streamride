spring:
  application:
    name: dashboard-service
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8083

logging:
  level:
    root: INFO
    com.streamride: DEBUG
    org.apache.kafka: WARN